import React from "react";

const Loading = () => {
  return (
    <div className="flex items-center flex-col justify-center p-6">
      <h1 className="font-lg font-bold">Loading...</h1>
    </div>
  );
};

export default Loading;
